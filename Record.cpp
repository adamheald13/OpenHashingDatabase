#include "Record.h"

Record::Record(string name, string address, int phoneNumber, float charge) {
  this->name = name;
  this->address = address;
  this->phoneNumber = phoneNumber;
  this->charge = charge;
}
